pdf-summarizer-backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # نقطه ورود FastAPI (Uvicorn)
│   ├── config.py               # تنظیمات پروژه (Env Variables, DB URL)
│   ├── database.py             # کانکشن SQLAlchemy و Session
│   ├── models.py               # لوکیشن Tableهای دیتابیس (SQLAlchemy Models)
│   ├── schemas.py              # ساختارهای ولیدیشن ورودی/خروجی (Pydantic Models)
│   ├── services/
│   │   ├── __init__.py
│   │   ├── pdf_processor.py    # اکسترکت کردن متن از PDF و Chunking
│   │   └── llm_service.py      # ارتباط با مدل خلاصه ساز (HuggingFace/Local)
│   └── routers/
│       ├── __init__.py
│       └── auth.py             # در صورت نیاز به مدیریت کاربران
│       └── summarizer.py       # Endpointهای آپلود PDF و دریافت خلاصه
├── notebooks/
│   └── train_qlora.ipynb       # جوپیتر نوتبوک کولاب برای فاین‌تیون مدل
├── scripts/
│   └── train.py                # اسکریپت پایتون فاین‌تیون (نسخه پروداکشن)
├── Dockerfile                  # دستورات ساخت ایمج داکر اپلیکیشن
├── docker-compose.yml          # برای بالا آوردن FastAPI و دیتابیس (MySQL/PostgreSQL) به صورت همزمان
├── requirements.txt            # پکیج‌های مورد نیاز
└── README.md